# Install this batch

Read `MAIN-BATCH1-FINAL-VALIDATION-20260821.md` for the exact patch list.

Read `DEPLOY-FULL.md` for the complete Chromebook upload, VPS tool check,
isolated preflight, transactional installation, verification, and rollback
workflow.

Use only:

```bash
bash ./install-main-batch1.sh --preflight
bash ./install-main-batch1.sh --install
```

The older release-history documents are retained as project history. They are
not the installation procedure for this batch.
