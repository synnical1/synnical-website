import { isBrowser } from "../utils/is.mjs";
import { defaultOptions } from "../utils/defaults.mjs";
import useStorage from "../createStorage/index.mjs";
//#region src/useLocalStorage/index.ts
function useLocalStorage(key, defaultValue, options = defaultOptions) {
	return useStorage(key, defaultValue, () => isBrowser ? localStorage : void 0, options);
}
//#endregion
export { useLocalStorage };
