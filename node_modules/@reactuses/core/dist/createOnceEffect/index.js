//#region src/createOnceEffect/index.ts
const record = /* @__PURE__ */ new WeakSet();
const createOnceEffect = (hook) => (effect, deps) => {
	const onceWrapper = () => {
		if (!record.has(effect)) {
			record.add(effect);
			return effect();
		}
	};
	hook(() => {
		return onceWrapper();
	}, deps);
};
//#endregion
exports.default = createOnceEffect;
