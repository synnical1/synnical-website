import _asyncToGenerator from "../_virtual/_@oxc-project_runtime@0.143.0/helpers/esm/asyncToGenerator.mjs";
import { useSupported } from "../useSupported/index.mjs";
import { useCallback } from "react";
//#region src/useEyeDropper/index.ts
const useEyeDropper = () => {
	const isSupported = useSupported(() => typeof window !== "undefined" && "EyeDropper" in window, true);
	return [isSupported, useCallback(_asyncToGenerator(function* (options = {}) {
		if (!isSupported) return { sRGBHex: "" };
		return new window.EyeDropper().open(options);
	}), [isSupported])];
};
//#endregion
export { useEyeDropper };
