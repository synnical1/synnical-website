import { isDev, isFunction } from "../utils/is.mjs";
import { useLatest } from "../useLatest/index.mjs";
import { useEffect } from "react";
//#region src/useUnmount/index.ts
function useUnmount(fn) {
	if (isDev) {
		if (!isFunction(fn)) console.error(`useUnmount expected parameter is a function, got ${typeof fn}`);
	}
	const fnRef = useLatest(fn);
	useEffect(() => () => {
		fnRef.current();
	}, [fnRef]);
}
//#endregion
export { useUnmount };
