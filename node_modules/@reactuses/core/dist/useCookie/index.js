const require_runtime = require("../_virtual/_rolldown/runtime.js");
const require_is = require("../utils/is.js");
const require_defaults = require("../utils/defaults.js");
const require_index = require("../useEventListener/index.js");
let react = require("react");
let js_cookie = require("js-cookie");
js_cookie = require_runtime.__toESM(js_cookie);
//#region src/useCookie/index.ts
const SAME_TAB_COOKIE_EVENT = "reactuse:cookie";
function getInitialState(key, defaultValue) {
	if (defaultValue !== void 0) return defaultValue;
	if (require_is.isBrowser) return js_cookie.default.get(key);
	if (process.env.NODE_ENV !== "production") console.warn("`useCookie` When server side rendering, defaultValue should be defined to prevent a hydration mismatches.");
	return "";
}
const useCookie = (key, options = require_defaults.defaultOptions, defaultValue) => {
	const [cookieValue, setCookieValue] = (0, react.useState)(getInitialState(key, defaultValue));
	require_index.useEventListener(SAME_TAB_COOKIE_EVENT, (event) => {
		const { key: evKey, value } = event.detail;
		if (evKey === key) setCookieValue(value);
	});
	(0, react.useEffect)(() => {
		const getStoredValue = () => {
			const raw = js_cookie.default.get(key);
			if (raw !== void 0 && raw !== null) return raw;
			else {
				if (defaultValue === void 0) js_cookie.default.remove(key);
				else js_cookie.default.set(key, defaultValue, options);
				return defaultValue;
			}
		};
		setCookieValue(getStoredValue());
	}, [
		defaultValue,
		key,
		JSON.stringify(options)
	]);
	return [
		cookieValue,
		(0, react.useCallback)((newValue) => {
			const value = require_is.isFunction(newValue) ? newValue(cookieValue) : newValue;
			if (value === void 0) js_cookie.default.remove(key);
			else js_cookie.default.set(key, value, options);
			setCookieValue(value);
			window.dispatchEvent(new CustomEvent(SAME_TAB_COOKIE_EVENT, { detail: {
				key,
				value
			} }));
		}, [
			key,
			cookieValue,
			JSON.stringify(options)
		]),
		(0, react.useCallback)(() => {
			const cookieValue = js_cookie.default.get(key);
			if (require_is.isString(cookieValue)) setCookieValue(cookieValue);
		}, [key])
	];
};
//#endregion
exports.useCookie = useCookie;
