const require_index = require("../useUpdate/index.js");
const require_index$1 = require("../useEventListener/index.js");
let react = require("react");
//#region src/useTextSelection/index.ts
const useTextSelection = () => {
	const [selection, setSelection] = (0, react.useState)(null);
	const forceUpdate = require_index.useUpdate();
	const handleSelectionChange = () => {
		setSelection(document.getSelection());
		forceUpdate();
	};
	require_index$1.useEventListener("selectionchange", handleSelectionChange, () => document);
	(0, react.useEffect)(() => {
		setSelection(document.getSelection());
	}, []);
	return selection;
};
//#endregion
exports.useTextSelection = useTextSelection;
