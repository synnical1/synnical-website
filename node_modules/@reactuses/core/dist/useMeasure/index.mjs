import { defaultOptions } from "../utils/defaults.mjs";
import { useResizeObserver } from "../useResizeObserver/index.mjs";
import { useState } from "react";
//#region src/useMeasure/index.ts
const defaultState = {
	x: 0,
	y: 0,
	width: 0,
	height: 0,
	top: 0,
	left: 0,
	bottom: 0,
	right: 0
};
const useMeasure = (target, options = defaultOptions) => {
	const [rect, setRect] = useState(defaultState);
	return [rect, useResizeObserver(target, (entries) => {
		if (entries[0]) {
			const { x, y, width, height, top, left, bottom, right } = entries[0].contentRect;
			setRect({
				x,
				y,
				width,
				height,
				top,
				left,
				bottom,
				right
			});
		}
	}, options)];
};
//#endregion
export { useMeasure };
