const require_index = require("../useEventListener/index.js");
let react = require("react");
//#region src/useWindowFocus/index.ts
function useWindowsFocus(defauleValue = false) {
	const [focused, setFocused] = (0, react.useState)(defauleValue);
	(0, react.useEffect)(() => {
		setFocused(window.document.hasFocus());
	}, []);
	require_index.useEventListener("blur", () => {
		setFocused(false);
	});
	require_index.useEventListener("focus", () => {
		setFocused(true);
	});
	return focused;
}
//#endregion
exports.useWindowsFocus = useWindowsFocus;
