import { defaultOptions } from "../utils/defaults.mjs";
import { useIntersectionObserver } from "../useIntersectionObserver/index.mjs";
import { useCallback, useState } from "react";
//#region src/useElementVisibility/index.ts
const useElementVisibility = (target, options = defaultOptions) => {
	const [visible, setVisible] = useState(false);
	const callback = useCallback((entries) => {
		const rect = entries[0].boundingClientRect;
		setVisible(rect.top <= (window.innerHeight || document.documentElement.clientHeight) && rect.left <= (window.innerWidth || document.documentElement.clientWidth) && rect.bottom >= 0 && rect.right >= 0);
	}, []);
	return [visible, useIntersectionObserver(target, callback, options)];
};
//#endregion
export { useElementVisibility };
