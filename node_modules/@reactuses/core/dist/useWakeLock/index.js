const require_asyncToGenerator = require("../_virtual/_@oxc-project_runtime@0.143.0/helpers/esm/asyncToGenerator.js");
const require_index = require("../useEvent/index.js");
const require_index$1 = require("../useSupported/index.js");
let react = require("react");
//#region src/useWakeLock/index.ts
const useWakeLock = (options = {}) => {
	const { onRequest, onRelease, onError } = options;
	const isSupported = require_index$1.useSupported(() => "wakeLock" in navigator);
	const [isActive, setIsActive] = (0, react.useState)(false);
	const sentinelRef = (0, react.useRef)(null);
	const requestedTypeRef = (0, react.useRef)(false);
	const forceRequest = require_index.useEvent(require_asyncToGenerator.default(function* () {
		if (!isSupported) return;
		try {
			var _sentinelRef$current;
			yield (_sentinelRef$current = sentinelRef.current) === null || _sentinelRef$current === void 0 ? void 0 : _sentinelRef$current.release();
			const sentinel = yield navigator.wakeLock.request("screen");
			sentinelRef.current = sentinel;
			setIsActive(true);
			sentinel.addEventListener("release", () => {
				var _sentinelRef$current$, _sentinelRef$current2;
				requestedTypeRef.current = (_sentinelRef$current$ = (_sentinelRef$current2 = sentinelRef.current) === null || _sentinelRef$current2 === void 0 ? void 0 : _sentinelRef$current2.type) !== null && _sentinelRef$current$ !== void 0 ? _sentinelRef$current$ : false;
				sentinelRef.current = null;
				setIsActive(false);
				onRelease === null || onRelease === void 0 || onRelease();
			}, { once: true });
			onRequest === null || onRequest === void 0 || onRequest();
		} catch (error) {
			onError === null || onError === void 0 || onError(error);
		}
	}));
	const request = require_index.useEvent(require_asyncToGenerator.default(function* () {
		if (!isSupported) return;
		if (document.visibilityState === "visible") yield forceRequest();
		else requestedTypeRef.current = "screen";
	}));
	const release = require_index.useEvent(require_asyncToGenerator.default(function* () {
		requestedTypeRef.current = false;
		const s = sentinelRef.current;
		sentinelRef.current = null;
		setIsActive(false);
		try {
			yield s === null || s === void 0 ? void 0 : s.release();
		} catch (error) {
			onError === null || onError === void 0 || onError(error);
		}
	}));
	(0, react.useEffect)(() => {
		const handleVisibilityChange = () => {
			if (document.visibilityState === "visible" && requestedTypeRef.current) {
				requestedTypeRef.current = false;
				forceRequest();
			}
		};
		document.addEventListener("visibilitychange", handleVisibilityChange);
		return () => {
			document.removeEventListener("visibilitychange", handleVisibilityChange);
		};
	}, [forceRequest]);
	(0, react.useEffect)(() => {
		return () => {
			requestedTypeRef.current = false;
			if (sentinelRef.current) {
				sentinelRef.current.release();
				sentinelRef.current = null;
			}
		};
	}, []);
	return {
		isSupported,
		isActive,
		request,
		release,
		forceRequest
	};
};
//#endregion
exports.useWakeLock = useWakeLock;
