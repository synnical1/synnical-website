const require_index = require("../useIsomorphicLayoutEffect/index.js");
let react = require("react");
//#region src/useSupported/index.ts
function useSupported(callback, sync = false) {
	const [supported, setSupported] = (0, react.useState)(false);
	(sync ? require_index.useIsomorphicLayoutEffect : react.useEffect)(() => {
		try {
			setSupported(Boolean(callback()));
		} catch (_unused) {
			setSupported(false);
		}
	}, []);
	return supported;
}
//#endregion
exports.useSupported = useSupported;
