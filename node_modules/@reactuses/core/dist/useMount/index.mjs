import { isDev, isFunction } from "../utils/is.mjs";
import { useEffect } from "react";
//#region src/useMount/index.ts
const useMount = (fn) => {
	if (isDev) {
		if (!isFunction(fn)) console.error(`useMount: parameter \`fn\` expected to be a function, but got "${typeof fn}".`);
	}
	useEffect(() => {
		fn === null || fn === void 0 || fn();
	}, []);
};
//#endregion
export { useMount };
