const require_is = require("../utils/is.js");
let react = require("react");
//#region src/useMount/index.ts
const useMount = (fn) => {
	if (require_is.isDev) {
		if (!require_is.isFunction(fn)) console.error(`useMount: parameter \`fn\` expected to be a function, but got "${typeof fn}".`);
	}
	(0, react.useEffect)(() => {
		fn === null || fn === void 0 || fn();
	}, []);
};
//#endregion
exports.useMount = useMount;
