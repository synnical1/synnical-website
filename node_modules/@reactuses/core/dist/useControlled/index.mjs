import { useLatest } from "../useLatest/index.mjs";
import { useCallback, useState } from "react";
//#region src/useControlled/index.ts
const useControlled = (value, defaultValue, onChange) => {
	const [stateValue, setStateValue] = useState(value !== void 0 ? value : defaultValue);
	const isControlled = value !== void 0;
	const onChangeRef = useLatest(onChange);
	const setValue = useCallback((newValue) => {
		var _onChangeRef$current;
		if (!isControlled) setStateValue(newValue);
		(_onChangeRef$current = onChangeRef.current) === null || _onChangeRef$current === void 0 || _onChangeRef$current.call(onChangeRef, newValue);
	}, [isControlled, onChangeRef]);
	return [isControlled ? value : stateValue, setValue];
};
//#endregion
export { useControlled };
