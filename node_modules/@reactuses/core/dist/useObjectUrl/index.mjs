import { useEffect, useState } from "react";
//#region src/useObjectUrl/index.ts
const useObjectUrl = (object) => {
	const [url, setUrl] = useState();
	useEffect(() => {
		if (object) setUrl(URL.createObjectURL(object));
		return () => {
			if (url) URL.revokeObjectURL(url);
		};
	}, [object]);
	return url;
};
//#endregion
export { useObjectUrl };
