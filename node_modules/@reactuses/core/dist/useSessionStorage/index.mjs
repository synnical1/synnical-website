import { isBrowser } from "../utils/is.mjs";
import { defaultOptions } from "../utils/defaults.mjs";
import useStorage from "../createStorage/index.mjs";
//#region src/useSessionStorage/index.ts
function useSessionStorage(key, defaultValue, options = defaultOptions) {
	return useStorage(key, defaultValue, () => isBrowser ? sessionStorage : void 0, options);
}
//#endregion
export { useSessionStorage };
