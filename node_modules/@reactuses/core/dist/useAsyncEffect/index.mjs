import { noop } from "../utils/is.mjs";
import { useMountedState } from "../useMountedState/index.mjs";
import _asyncToGenerator from "../_virtual/_@oxc-project_runtime@0.143.0/helpers/esm/asyncToGenerator.mjs";
import { useEffect } from "react";
//#region src/useAsyncEffect/index.ts
const useAsyncEffect = (effect, cleanup = noop, deps) => {
	const mounted = useMountedState();
	useEffect(() => {
		(function() {
			var _ref = _asyncToGenerator(function* () {
				if (!mounted()) return;
				yield effect();
			});
			return function execute() {
				return _ref.apply(this, arguments);
			};
		})()();
		return () => {
			cleanup();
		};
	}, deps);
};
//#endregion
export { useAsyncEffect };
